# Beep.OilandGas.Client

## Snapshot

- Category: Core/client abstractions
- Scan depth: Medium
- Current role: shared client-side abstractions and integration support
- Maturity signal: support library rather than primary business domain

## Observed Structure

- Top-level folders: `App`, `Authentication`, `Connection`, `DependencyInjection`, `Exceptions`, `Models`
- The project includes implementation templates, developer guidance, and service-status notes, which suggests it supports broader client patterns rather than end-user workflows directly

## Representative Evidence

- Client dependency support: `DependencyInjection/`
- Auth and connection support: `Authentication/`, `Connection/`
- Guidance files: `CLIENT_IMPLEMENTATION_TEMPLATE.md`, `DEVELOPER_GUIDE.md`, `SERVICE_IMPLEMENTATION_STATUS.md`

## Planning Notes

- Phase 6 and 10 should keep this project in mind when trimming direct web references so shared client abstractions are not bypassed accidentally.
- If typed-client contracts are formalized further, this project is a candidate place to centralize shared client-side patterns.
- `Beep.OilandGas.Client/App/Services/Analysis/AnalysisService.GasLift.cs` should treat the live API as wrapper-based: `GetGasLiftPerformanceAsync` must use `/api/gaslift/performance/{wellUWI}`, `OptimizeInjectionAsync` now maps to `/api/gaslift/analyze-potential` with the repo's default `100..5000` Mscf/day analysis range and 50 points, and `DesignGasLiftAsync` now maps to `/api/gaslift/design-valves` with the repo's five-valve default plus a bounded casing-rating-based injection pressure; the old valve-spacing and troubleshoot calls remain a separate audit because they still lack a direct live owner.
- `Beep.OilandGas.Client/App/Services/Analysis/AnalysisService.Pipeline.cs` should treat the live API as `/api/pipelineanalysis/*`; `AnalyzePipelineAsync` now maps `PipelineAnalysisResult` back into the legacy `PIPELINE_FLOW_ANALYSIS_RESULT`, `CalculatePressureDropAsync` now maps `GAS_PIPELINE_FLOW_PROPERTIES` onto `CalculatePressureDropRequest` and folds `PressureDropResult` back into the legacy result contract, while the old capacity/integrity/design assumptions remain a separate compatibility audit.
- `Beep.OilandGas.Client/App/Services/Analysis/AnalysisService.WellTest.cs` now routes legacy build-up, drawdown, derivative, and interpretation calls through `/api/calculations/well-test` by wrapping `WELL_TEST_DATA` into `WellTestAnalysisCalculationRequest`; only the old raw-history method remains a separate audit because the live API surface exposes analysis history, not raw `WELL_TEST_DATA` history, one-for-one.
- `Beep.OilandGas.Client/App/Services/Analysis/AnalysisService.Compressor.cs` now has a live API owner again for its legacy post routes because `Beep.OilandGas.ApiService/Controllers/CompressorController.cs` restores `/api/compressor/analyze`, `/api/compressor/power`, and the centrifugal/reciprocating design posts as thin calculator-backed compatibility shims; the old `GET /api/compressor/{id}/performance` read still lacks a persisted owner and stays separate.
- `Beep.OilandGas.Client/App/Services/Analysis/AnalysisService.Choke.cs` still points at a dead legacy route family, but that audit was intentionally left unresolved because the client payloads do not carry the choke configuration needed to honestly rebuild the old API surface without inventing defaults.
- `Beep.OilandGas.Web/Pages/PPDM39/Reservoir/{ReservoirOverview,ReservesClassification,EORScreening}.razor` now bind the live `ReservoirDashboardSummary` and `ReservoirPoolDto` contracts from `Beep.OilandGas.Models.Data.Production` instead of keeping page-local copies of the reservoir dashboard response shape; keep those pages aligned to the field-scoped `ReservoirController` summary/pools endpoints rather than letting local DTOs drift.
- `Beep.OilandGas.ApiService/Controllers/WellController.cs` now restores the legacy well lookup routes `/api/well/uwi/{uwi}` and `/api/well/uwi/{uwi}/status` as thin `WellServices` compatibility shims, which keeps `WellsIndex.razor`, `Wells/Search.razor`, `WellDetails/Search.razor`, and `WellDetails/Status.razor` working without bypassing the typed well owner.
- `Beep.OilandGas.Web/Services/ComplianceServiceClient.cs` / `IComplianceServiceClient.cs` now own the web-layer contract for `/api/field/current/compliance/*`; `ComplianceDashboard.razor`, `ObligationDetail.razor`, `PaymentDialog.razor`, and `GHGReport.razor` should stay on that typed client so route strings, query encoding, and 404-as-null obligation lookup behavior remain centralized.
- `Beep.OilandGas.Web/Pages/PPDM39/HSE/ComplianceCalendar.razor` should stay on `ComplianceServiceClient` for both obligation list and create flows; keep the live `/api/field/current/compliance/obligations` route family out of page-local `ApiClient` calls and leave only current-field summary/context lookups at the page boundary.
- `Beep.OilandGas.Web/Pages/PPDM39/BusinessProcess/ComplianceObligations.razor` should stay on the domain compliance route family, not the workflow `/api/field/current/process/compliance/obligations` summaries; the user-facing obligations calendar needs the richer `ObligationSummary` data from `ComplianceServiceClient`, and the page should not ship hardcoded overdue defaults.
- `Beep.OilandGas.Web/Pages/PPDM39/BusinessProcess/HSEIncidentDetail.razor` now uses the shared HSE process request models that match `HSEProcessController` (`HSEIncidentUpdateRequest.RcaSummary`, `CorrectiveActionCloseRequest.Notes`, `IncidentCloseRequest.Reason`) through `IBusinessProcessServiceClient` instead of page-local raw route strings. Keep those HSE process request contracts in shared models under `Beep.OilandGas.Models/Data/Process`, and keep corrective-action creation on the live `/actions` route rather than the stale page-local `/cas` path.
- `Beep.OilandGas.Web/Services/HSEServiceClient.cs` / `IHSEServiceClient.cs` now own the live `/api/field/current/hse/*` incident, KPI, and HAZOP route family used by `Pages/PPDM39/BusinessProcess/HSEIncidents.razor`, `Pages/PPDM39/HSE/HSEDashboard.razor`, `Pages/PPDM39/HSE/IncidentDetail.razor`, and `Pages/PPDM39/HSE/HAZOPStudy.razor`. Those pages should stay on the typed client so they do not duplicate raw route strings, do not fetch field ids the `HSEController` already derives from `IFieldOrchestrator`, keep HSE transition/cause/corrective-action posts on the boolean `POST` path for the controller's `204 No Content` endpoints, and keep HAZOP node/deviation route encoding centralized.
- `Beep.OilandGas.ApiService/Controllers/BusinessProcess/GateReviewController.cs` now resolves gate checklists from the gate instance's `ProcessId` and shares `GateChecklistResponse` through `Beep.OilandGas.Models/Data/Process`; the gate detail route was previously treating an instance id like a definition id.
- `Beep.OilandGas.Web/Services/BusinessProcessServiceClient.cs` / `IBusinessProcessServiceClient.cs` now own the active `/api/field/current/process*`, `/api/field/current/gates*`, process analytics, and workflow HSE incident route families used by the process/gate/analytics pages. Keep those pages on the typed client so gate decisions and workflow HSE RCA/close/action endpoints stay on the boolean `POST`/`PATCH` paths for 204 responses, the process dashboard keeps its open-incident KPI off page-local route strings, and the workflow HSE detail page does not drift back to the stale `/cas` route.
- `Beep.OilandGas.Web/Pages/PPDM39/BusinessProcess/ComplianceReport.razor` should stay on `IBusinessProcessServiceClient.SubmitComplianceReportAsync` for `/api/field/current/process/compliance/reports` instead of posting raw route strings directly. The page also now loads `FieldLifecycleSummary` for its header so the active field name is not blank while the report workflow remains field-scoped.
- `Beep.OilandGas.Web/Pages/PPDM39/Accounting/VolumeReconciliation.razor` and `Controllers/Accounting/Allocation/AllocationController.cs` should keep the reconciliation contract typed end-to-end: the page must send the active `FieldId` in `VolumeReconciliationRequest`, and the controller should return the shared `VolumeReconciliationResult` directly instead of anonymous JSON that stringifies `Status` and erases the nested `OilVolume`/`GasVolume` and `Issues` types.
- `Beep.OilandGas.Web/Pages/PPDM39/Accounting/CostAllocation.razor` and `Controllers/Accounting/Cost/CostAllocationController.cs` should keep the cost-allocation contract typed end-to-end as well: the page must send the active `FieldId` in `CostAllocationRequest`, and the controller should return `CostAllocationComputationResult` directly so the page renders the real `AllocationDetails` breakdown instead of trying to coerce an unrelated anonymous payload into `CostAllocationResult`.
- `Beep.OilandGas.Web/Pages/PPDM39/Accounting/Royalties.razor` and `Components/RoyaltyCalculationDialog.razor` should keep the royalties workflow field-scoped: the list query should carry the active `fieldId`, and the dialog should post the shared `CalculateRoyaltyRequest` with that same field id via the boolean `POST` path because the controller requires `FieldId` even though the dialog only needs success/failure.
- `Beep.OilandGas.Web/Pages/PPDM39/Accounting/ProductionAccounting.razor` should no longer fake period workflow locally: the page now generates recent periods dynamically, loads summary state from the field-scoped accounting controller with a selected `periodEnd`, and closes periods through `/api/field/current/accounting/close-period` using the shared `CloseAccountingPeriodRequest` contract instead of flipping a local `_closeStatus` string.
- `Controllers/Field/AccountingController.cs` should source the dashboard recent-activities feed and production revenue lines from field-linked `REVENUE_TRANSACTION` data through `ProductionAccountingService`, not from the global sales/receivables helpers. The summary endpoint now intentionally leaves receivable aggregates at zero until there is an honest field-linked receivables owner instead of leaking cross-field balances through a field-scoped page.
- `Beep.OilandGas.Web/Services/AccountingServiceClient.cs` / `IAccountingServiceClient` now form a small live owner for the field-scoped accounting route family that is actually in use: recent activities, production summary, revenue lines, and close-period. `AccountingDashboard.razor`, `ProductionAccounting.razor`, `EconomicAnalysis.razor`, and `EconomicEvaluation.razor` should stay off raw `/api/field/current/accounting/recent-activities`, `/production-summary`, `/revenue-lines`, and `/close-period` strings, and shared accounting response models should stay in `Beep.OilandGas.Models/Data/Accounting` rather than in controller-local or page-local classes.
- `Beep.OilandGas.Web/Services/HSEServiceClient.cs` / `IHSEServiceClient.cs` also own the incident/KPI reads and incident-report post used by `Pages/PPDM39/HSE/IncidentManagement.razor`; keep that page off raw `/api/field/current/hse/incidents` and `/api/field/current/hse/kpi` strings and on the shared `HSEIncidentRecord`, `HSEKPISet`, and `ReportIncidentRequest` contracts.
- `Beep.OilandGas.Web/Services/ProductionServiceClient.cs` / `IProductionServiceClient` should own the stored test lookup used by `Pages/PPDM39/Calculations/WellTestAnalysis.razor`; keep that page off the legacy raw `/api/production/field/wells/{wellId}/tests` path and use `GetWellTestsAsync` so the production well-test route family stays centralized.
- `Beep.OilandGas.Web/Services/WellStatusServiceClient.cs` / `IWellStatusServiceClient` now own the web-layer contract for the live well-status facet routes used by `Pages/PPDM39/WellDetails/SetWellStatus.razor` and `Status.razor`; keep those pages off raw `/api/wellstatus/*` strings and on the centralized current-status, page-data, qualifier, history, and set-facet calls.
- `Beep.OilandGas.Web/Services/WellLookupServiceClient.cs` / `IWellLookupServiceClient` now own the legacy compatibility lookup route `/api/well/uwi/{uwi}` used by `Pages/PPDM39/WellsIndex.razor`, `Wells/Search.razor`, `WellDetails/Search.razor`, and the well-header lookup in `WellDetails/SetWellStatus.razor`; keep those pages off raw lookup strings and use the focused client instead of reaching into the compatibility route directly.
- `Beep.OilandGas.Web/Services/LifeCycleService.cs` / `ILifeCycleService` should be the page-boundary owner for generic current-field context reads. Summary-only and current-field-only pages should use `GetFieldLifecycleSummaryAsync()` and `GetCurrentFieldAsync()` instead of repeating raw `/api/field/current/summary` and `/api/field/current` `ApiClient` calls. The verified migrated set now includes the well search/status pages, `HSE/IncidentManagement.razor`, `HSE/ComplianceCalendar.razor`, the accounting header/context pages, `Properties/HeatMap.razor`, `Operations/{LeaseAcquisition,EnhancedRecovery}.razor`, `Production/ProductionForecasting.razor`, `BusinessProcess/ProcessAnalyticsDashboard.razor`, several calculation workbenches, and the decommissioning header pages.
- `Beep.OilandGas.Web/Services/IDecommissioningServiceClient.cs` / `DecommissioningServiceClient.cs` now own the live field-scoped decommissioning list, abandon-well, and cost-estimation routes used by `Pages/PPDM39/Decommissioning/WellAbandonment.razor` and `CostEstimation.razor`; keep those pages off raw `/api/field/current/decommissioning/*` strings, keep the user-id query encoding in the client for `abandon-well`, and keep cost estimation bound to the typed `DecommissioningCostEstimateResponse` properties instead of reflection-based guessed names.
- `Beep.OilandGas.Web/Services/IDevelopmentServiceClient.cs` / `DevelopmentServiceClient.cs` now own the field-scoped development well/facility lookup routes used by the calculations, pumps, and reservoir workbenches. `ChokeAnalysis.razor`, `PumpPerformance.razor`, `GasLift.razor`, `DeclineCurveAnalysis.razor`, `NodalAnalysis.razor`, `WellTestAnalysis.razor`, `HydraulicPump.razor`, `SuckerRodPumping.razor`, `PlungerLift.razor`, `CharacterizationSummary.razor`, and `CompressorAnalysis.razor` should stay off raw `/api/field/current/development/wells` and `/api/field/current/development/facilities` strings; keep any page-local display DTOs as thin mappings from the shared `WELL` / `FACILITY` models rather than duplicating route ownership in the page.
- `Beep.OilandGas.Web/Services/AfeServiceClient.cs` / `IAfeServiceClient.cs` now own the live `/api/accounting/cost/afe*` route family for `Pages/PPDM39/Economics/AFEManagement.razor`; keep the page on that focused client, and use the repository-backed `AfeId` for approve/reject calls rather than the display `AfeNumber` routed through the grid.
