@page "/ppdm39/database-setup"
@inject NavigationManager NavigationManager

@code {
    protected override void OnInitialized()
    {
        NavigationManager.NavigateTo("/ppdm39/setup", replace: true);
    }
}
