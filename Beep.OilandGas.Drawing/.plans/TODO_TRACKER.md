# Todo Tracker

## Purpose

This is the execution tracker for the enhancement plan. Update this file as work begins, completes, slips, or changes priority.

## Status Legend

- `todo` - not started
- `in-progress` - actively being implemented
- `blocked` - waiting on design, dependency, or data
- `done` - completed and verified

## Program Backlog

| ID | Phase | Status | Priority | Task | Notes |
| --- | --- | --- | --- | --- | --- |
| P1-01 | 01 | done | high | Define map-scene and section-scene contracts | `DrawingScene` and viewport-scene integration added |
| P1-02 | 01 | done | high | Introduce typed CRS and unit services | Typed CRS, units, and `DepthTransform` now replace legacy coordinate handling |
| P1-03 | 01 | done | high | Add validation rules for missing CRS, units, and invalid geometry | Scene and normalized-model diagnostics added |
| P1-04 | 01 | done | medium | Add shared legend, scale-bar, north-arrow, and annotation primitives | Reusable overlay primitives added under `Primitives/` |
| P2-01 | 02 | done | high | Replace simplified deviated well fallback with survey-driven path rendering | Enhanced renderer, builder, and PPDM schematic data path now use `DeviationSurvey` |
| P2-02 | 02 | done | high | Add completion-string and equipment overlay rendering | Survey-aligned casing, tubing, and perforation overlays are in; legacy SVG-backed equipment resolution is in; survey-following string geometry is now persisted back into tubing and casing model surfaces |
| P2-03 | 02 | done | medium | Add engineering annotation profiles | Kickoff and landing survey markers, perforation and equipment callouts, and compact/detailed/print profile behavior are now in |
| P3-01 | 03 | done | high | Build multi-track log renderer | Configurable track definitions, grouped curve tracks, track headers, a dedicated depth track, PWLS-driven standard petroleum templates, per-track scale annotations, density-neutron crossover shading, log-decade grid treatment, drawing-engine integration via `WellLogLayer` and `WellLogBuilder`, and loader-backed sample/gallery integration via `WellLogGallery` are in |
| P3-02 | 03 | done | high | Make PWLS mnemonic normalization part of ingestion | Shared ingestion-time PWLS normalization now runs across LAS, DLIS, WITSML, CSV, and database loaders; PWLS display names are populated consistently and curve filtering can match original or standardized names |
| P3-03 | 03 | done | medium | Add lithology strips and zonation panels | `LogData` now carries lithology and zone intervals, standard templates surface interval tracks automatically when present, and `LogRenderer` renders lithology strips with existing pattern fills plus labeled zonation panels |
| P3-04 | 03 | done | medium | Add multi-well correlation layouts | `MultiWellCorrelationLayer` and `WellCorrelationBuilder` now render side-by-side well panels on a shared depth window with aligned zone or explicit markers, correlation fences, and per-panel headers |
| P4-01 | 04 | done | high | Define grid and surface models | `ReservoirData` now carries typed grids and surfaces, `ResqmlReservoirLoader` hydrates them from RESQML representations, and normalized-data validation covers contour-ready geometry contracts |
| P4-02 | 04 | done | high | Implement contour generation and labeling | `ContourGenerator` now produces interpolated isoline segments from typed reservoir surfaces, `ReservoirContourLayer` renders major/minor contours with labels, and `ReservoirContourBuilder` supports direct-data or loader-backed contour scenes |
| P4-03 | 04 | done | high | Add reservoir map renderer with well and fault overlays | `ReservoirMapBuilder` now composes contour bases with fault traces derived from typed fault surfaces and explicit well map overlays via `ReservoirMapOverlayLayer` |
| P4-04 | 04 | done | medium | Build cross-section extractor and renderer | `ReservoirCrossSectionBuilder` now extracts section profiles from typed reservoir surfaces, projects wells onto the cut line, and renders surfaces plus fluid contacts in section view |
| P5-01 | 05 | done | medium | Add field map layer engine | `FieldMapBuilder`, `FieldMapLayer`, typed `FieldMapData`, map scenes, and legend-driven plan-view rendering are now in for wells, fields, pools, leases, and facilities |
| P5-02 | 05 | done | medium | Add seismic footprint overlays | `Seismic3D` footprints and `Seismic2D` line overlays now render through the shared field-map engine |
| P5-03 | 05 | done | medium | Add facility and surface-system renderer | Explicit `FieldMapConnectionAsset` flowlines, gathering lines, export lines, and facility links now render as first-class surface-network connections with labels and arrows |
| P5-04 | 05 | done | medium | Add HSE and land-right overlays | Typed `LandRight`, `ProtectedArea`, and `HazardZone` overlays now reuse the field-map engine for compliance and risk map views |
| P6-01 | 06 | done | high | Add SVG and PDF export | `DrawingEngine` now exposes a shared `RenderToCanvas` path and `PdfExporter` plus `SvgExporter` now reuse the core layer pipeline for vector and document output |
| P6-02 | 06 | done | high | Add regression scenes and golden-image tests | `Beep.OilandGas.Drawing.Tests` now carries five deterministic regression scenes with pinned SHA256 hashes; runtime execution is policy-blocked on this workstation, but the harness and manifest are complete |
| P6-03 | 06 | done | medium | Add benchmark suite for logs, maps, and sections | `Beep.OilandGas.Drawing.Benchmarks` now benchmarks canonical sample-gallery scenes in-process for build and PNG render baselines, emits JSON reports, and has passed a smoke run against `WellLog_Petrophysical` |
| P6-04 | 06 | done | medium | Add sample gallery and usage docs | `DrawingSampleGallery` now exposes five canonical engineering scenes, `WellLogGallery` now exports standard petrophysical scenes to PNG/SVG/PDF, and the README documents gallery plus regression workflow usage |
| P6-05 | 06 | done | medium | Add GeoJSON and georeferenced map export | `GeoJsonExporter` now emits GeoJSON for typed field maps plus reservoir contour/fault/well overlays, `GeoReferencedImageExporter` now writes PNG + world-file + CRS sidecars, and reservoir contour/map builders now attach map scenes with CRS and world bounds |
| P6-06 | 06 | done | medium | Add scene-aware measurement API | `SceneMeasurementService` now computes distance and area in scene units for map, section, and depth views, `DrawingEngine` now exposes screen-to-world measurement helpers for interaction hosts, and focused tests cover projected, geographic, and depth constraints |
| P6-07 | 06 | done | medium | Add hover and selection hit-testing contract | `DrawingEngine.HitTest` now queries optional interactive layers in z-order, `FieldMapLayer` and `ReservoirMapOverlayLayer` now return typed feature hits, and focused tests cover point, polygon, and reservoir well selection paths |
| P6-08 | 06 | done | medium | Add scene-owned interaction persistence hooks | `DrawingScene.InteractionState` now stores typed selection and measurement annotations, and `DrawingEngine.RecordSelection` plus `RecordMeasurement` persist interaction artifacts without requiring a separate host-only state model |
| P6-09 | 06 | done | medium | Build interactive sample host | `Beep.OilandGas.Web` now exposes `/ppdm39/data-management/drawing-samples`, a Blazor workbench backed by `DrawingSampleGallery` that renders live scene images, supports typed selection plus distance/area workflows, and links from the Data Management hub; full web-project build validation is currently blocked by pre-existing package restore errors unrelated to this host |
| P6-10 | 06 | done | medium | Extend hit testing into reservoir contours | `ReservoirContourLayer` now implements `IInteractiveLayer`, reuses generated contour segments for nearest-line hit resolution, returns typed contour metadata, and focused tests validate contour selection through `DrawingEngine.HitTest` |
| P6-11 | 06 | done | medium | Attach depth scene and interaction to well schematics | `WellSchematicBuilder` now attaches a typed depth scene with world bounds and well metadata, `EnhancedWellSchematicRenderer` now implements `IInteractiveLayer` for equipment and perforation selection, and focused tests validate that the canonical schematic sample resolves a packer hit through `DrawingEngine.HitTest` |
| P6-12 | 06 | done | medium | Make schematic callout labels interactive | `AnnotationRenderer` now exposes prepared callout label geometry for equipment and perforation annotations, `EnhancedWellSchematicRenderer` now hit-tests those shared label bounds so the right-side callout lane resolves the same typed features as the downhole bodies, and focused drawing regressions now compile and can run locally despite intermittent Windows Application Control interference on this workstation |
| P6-13 | 06 | done | medium | Attach depth scene and first hit path to well logs | `WellLogBuilder` now attaches a typed depth scene with log metadata, `WellLogLayer` now renders in true depth-space coordinates and implements `IInteractiveLayer` for track-level hits, and a focused regression validates both track selection and depth-aware distance measurement |
| P6-14 | 06 | done | medium | Render persisted interaction artifacts through exports | `DrawingEngine.RenderToCanvas` now draws persisted selection and measurement annotations from `DrawingScene.InteractionState`, so PNG/SVG/PDF exports inherit the same visuals as the live scene, focused render regressions cover selection and measurement pixels, and the sample host now keeps only draft overlays while persisted artifacts come from the engine image |
| P6-15 | 06 | done | medium | Add exact well-log curve and interval hits | `LogRenderer` now exposes renderer-owned interaction layouts for curve polylines and interval bands, `WellLogLayer` now resolves exact curve and lithology/zone interval hits before falling back to track hits, interval-only configured tracks survive renderer filtering, and focused regressions cover track fallback plus exact curve and interval selection |
| P6-16 | 06 | done | medium | Add configurable styling hooks for persisted annotations | `SceneInteractionState` now owns a public `SceneInteractionRenderStyle`, `SceneInteractionRenderer` consumes that style for visibility, colors, radii, dash patterns, label offsets, and text sizing, and focused render regressions cover both custom styling and hidden persisted annotations |
| P6-17 | 06 | done | medium | Benchmark interaction render and hit-test hot paths | `Beep.OilandGas.Drawing.Benchmarks` now supports `render-png-annotated` and `hit-test` operations, benchmark-specific interaction profiles seed persisted measurements and typed selection probes without polluting the shared sample-gallery contract, and a focused smoke run succeeded for `FieldMap_AssetNetwork` plus `WellLog_Petrophysical` |
| P6-18 | 06 | done | medium | Add composite density-neutron crossover hits | `LogRenderer` now exposes density-neutron crossover polygons from the same segment builder used for crossover shading, `WellLogLayer` resolves those composite regions before single-curve hits, and a focused regression validates typed crossover selection with curve and depth-span metadata |
| P6-19 | 06 | done | medium | Expose annotation style presets in sample host | `DrawingSampleHost` now surfaces Auto, Default, Map, Log, Schematic, and Hidden annotation presets through `MudChipSet`, applies them by mutating the active scene's `SceneInteractionRenderStyle`, rerenders the engine image immediately, and validates cleanly in file diagnostics while full web build remains blocked by the pre-existing package-feed and API restore issues |
| P6-20 | 06 | done | medium | Add vector-export benchmark operations | `Beep.OilandGas.Drawing.Benchmarks` now supports `render-svg` and `render-pdf`, measuring real vector export payloads through `SvgExporter.ExportToString` plus the existing PDF file export path, and a focused smoke run succeeded for `FieldMap_AssetNetwork` plus `WellLog_Petrophysical` |
| P6-21 | 06 | done | medium | Add annotated vector-export benchmark operations | `Beep.OilandGas.Drawing.Benchmarks` now supports `render-svg-annotated` and `render-pdf-annotated`, reuses benchmark-local persisted-annotation setup from `SceneBenchmarkInteractionProfiles`, and a focused smoke run succeeded for `FieldMap_AssetNetwork` plus `WellLog_Petrophysical` |
| P6-22 | 06 | done | medium | Close sample-host export workflow gap | `DrawingSampleHost` now exposes PNG, SVG, and PDF export actions that download the current engine state through the shared `IClientFileExportService`, so viewport state plus persisted selections and measurements flow through the same live/export path; touched files validate cleanly while full web build remains blocked by the pre-existing restore issues |
| P6-23 | 06 | done | medium | Add drag-based pan to sample host | `DrawingSampleHost` now treats canvas drag as viewport pan through `DrawingEngine.Pan`, suppresses post-drag click selection/measurement events, surfaces current pan offsets in the canvas header, and falls back to `ResetViewport()` for render-only samples so Reset View no longer no-ops when no typed scene is attached; touched files validate cleanly while full web build remains blocked by the pre-existing restore issues |
| P6-24 | 06 | done | medium | Add targeted delete actions for persisted annotations | `SceneInteractionState` now exposes `RemoveSelection` and `RemoveMeasurement`, `DrawingSampleHost` can remove the current persisted selection or an individual persisted measurement without clearing everything, focused `SceneInteractionStateTests` removal cases passed, and touched web files validate cleanly while full web build remains blocked by the pre-existing restore issues |
| P6-25 | 06 | done | medium | Add georeferenced PNG bundle export to sample host | `DrawingSampleHost` now exposes a map-only georeferenced PNG export that zips the PNG with its world file and CRS sidecar using `GeoReferencedImageExporter`, so map workflows can download exchange-ready raster bundles directly from the live host without widening the scene contract |
| P6-26 | 06 | done | medium | Add explicit sample-scene supplemental export contract | `DrawingSampleScene` now carries explicit `SupplementalExports`, `DrawingSampleGallery` declares a Field Map GeoJSON export through that contract, focused `DrawingSampleGalleryTests` passed through `dotnet test --filter FullyQualifiedName~DrawingSampleGalleryTests`, and the host now surfaces scene-declared exports without guessing from names or layers |
| P6-27 | 06 | done | medium | Add reservoir contour GeoJSON supplemental export | `DrawingSampleGallery` now declares a Reservoir Contour GeoJSON export through `SupplementalExports`, focused `DrawingSampleGalleryTests` now validate both Field Map and Reservoir Contour GeoJSON actions through `dotnet test --filter FullyQualifiedName~DrawingSampleGalleryTests`, and the host surfaces the new action automatically without any new scene-name heuristics |
| P6-28 | 06 | done | medium | Unify georeferenced PNG under supplemental export contract | Field Map georeferenced PNG is now declared as a `DrawingSampleScene.SupplementalExports` zip bundle beside Field Map GeoJSON, `DrawingSampleHost` no longer special-cases georeferenced export in host code, focused `DrawingSampleGalleryTests` now validate both GeoJSON and georeferenced zip outputs, and touched files validate cleanly in editor diagnostics |
| P6-29 | 06 | done | medium | Improve supplemental export presentation in sample host | `DrawingSampleHost` now styles scene-declared export buttons based on artifact type and renders each export action's description under the export section, so GeoJSON and zip-bundle actions read as intentional workflow options instead of generic attachment buttons |
| P6-30 | 06 | done | medium | Add reservoir contour georeferenced bundle export | `DrawingSampleGallery` now declares a Reservoir Contour georeferenced PNG zip bundle through `SupplementalExports`, focused `DrawingSampleGalleryTests` validate both its GeoJSON and georeferenced raster outputs, and the same host workflow now covers exchange-ready contour bundles without any new host-only logic |
| P6-31 | 06 | done | medium | Add reservoir cross-section JSON supplemental export | `DrawingSampleGallery` now declares a Reservoir Cross Section JSON artifact through `SupplementalExports`, focused `DrawingSampleGalleryTests` validate both the contract declaration and the typed section payload, and the host surfaces the new engineering-data export without any host-specific cross-section logic |
| P6-32 | 06 | done | medium | Add well schematic JSON supplemental export | `DrawingSampleGallery` now declares a Well Schematic JSON artifact through `SupplementalExports`, focused `DrawingSampleGalleryTests` validate both the contract declaration and the typed well payload, and the host can now surface schematic engineering data through the same scene-declared workflow used by maps and sections |
| P6-33 | 06 | done | medium | Add well log JSON supplemental export | `DrawingSampleGallery` now declares a Well Log JSON artifact through `SupplementalExports`, focused `DrawingSampleGalleryTests` validate both the contract declaration and the typed petrophysical payload, and the host can now surface engineering-data exports for all five canonical gallery scenes without any scene-specific host logic |

## Immediate Sequence

1. Decide whether future correlation, composite, or multi-scene workflows should declare their own supplemental exports through the new `DrawingSampleScene.SupplementalExports` contract.
2. Decide whether touch-friendly pan and pinch-zoom input belongs in the same host or in a broader reusable drawing-workbench surface.
3. Decide whether `DrawingSampleExportAction` should grow first-class display metadata instead of relying on host-side content-type heuristics for button styling.

## Deferred Until Core Is Stable

- Full seismic trace rendering.
- 3D reservoir visualization.
- CAD-oriented exports.
- Real-time streaming viewers.