namespace Beep.OilandGas.ProductionAccounting.Constants
{
    /// <summary>Stable <c>ROW_CREATED_BY</c> / insert user ids when no interactive principal exists (batch, stubs).</summary>
    public static class ProductionAccountingAuditActors
    {
        public const string System = "system";
    }
}
