CREATE TABLE `LEAD`
(
    `LEAD_ID` VARCHAR(40) NOT NULL,
    `LEAD_NAME` VARCHAR(255),
    `LEAD_SHORT_NAME` VARCHAR(255),
    `FIELD_ID` VARCHAR(40),
    `PLAY_ID` VARCHAR(40),
    `LEAD_STATUS` VARCHAR(50),
    `DESCRIPTION` VARCHAR(2000),
    `LEAD_SOURCE` VARCHAR(40),
    `INITIAL_ASSESSMENT_DATE` DATETIME,
    `PROMOTED_TO_PROSPECT_DATE` DATETIME,
    `REJECTED_DATE` DATETIME,
    `REJECTION_REASON` VARCHAR(255),
    `LATITUDE` DECIMAL(18,2),
    `LONGITUDE` DECIMAL(18,2),
    `ELEVATION` DECIMAL(18,2),
    `ELEVATION_OUOM` VARCHAR(255),
    `LOCATION_DESCRIPTION` VARCHAR(2000),
    `INITIAL_RISK_ASSESSMENT` VARCHAR(255),
    `INITIAL_VOLUME_ESTIMATE_OIL` DECIMAL(18,2),
    `INITIAL_VOLUME_ESTIMATE_GAS` DECIMAL(18,2),
    `INITIAL_VOLUME_ESTIMATE_OUOM` VARCHAR(255),
    `PROMOTED_TO_PROSPECT_ID` VARCHAR(40),
    `ACTIVE_IND` VARCHAR(1) DEFAULT 'Y',
    `EFFECTIVE_DATE` DATETIME,
    `EXPIRY_DATE` DATETIME,
    `PPDM_GUID` VARCHAR(40),
    `REMARK` VARCHAR(2000),
    `SOURCE` VARCHAR(40),
    `ROW_CHANGED_BY` VARCHAR(30),
    `ROW_CHANGED_DATE` DATETIME,
    `ROW_CREATED_BY` VARCHAR(30),
    `ROW_CREATED_DATE` DATETIME,
    `ROW_EFFECTIVE_DATE` DATETIME,
    `ROW_EXPIRY_DATE` DATETIME,
    `ROW_QUALITY` VARCHAR(40)
);
