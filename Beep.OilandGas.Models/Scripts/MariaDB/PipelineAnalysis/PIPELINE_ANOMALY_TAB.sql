CREATE TABLE `PIPELINE_ANOMALY`
(
    `PIPELINE_ID` VARCHAR(40),
    `ANOMALY_ID` VARCHAR(40),
    `ANOMALY_TYPE` VARCHAR(50),
    `INSPECTION_ID` VARCHAR(40),
    `LOCATION_STATION_MILEPOST` DECIMAL(18,2) NOT NULL,
    `LOCATION_STATION_MILEPOST_OUOM` VARCHAR(255),
    `SEVERITY` VARCHAR(255),
    `ANOMALY_LENGTH` DECIMAL(18,2) NOT NULL,
    `ANOMALY_LENGTH_OUOM` VARCHAR(255),
    `ANOMALY_DEPTH` DECIMAL(18,2) NOT NULL,
    `ANOMALY_DEPTH_OUOM` VARCHAR(255),
    `ANOMALY_WIDTH` DECIMAL(18,2) NOT NULL,
    `ANOMALY_WIDTH_OUOM` VARCHAR(255),
    `SEGMENT_ID` VARCHAR(40),
    `DESCRIPTION` VARCHAR(2000),
    `ACTIVE_IND` VARCHAR(1) DEFAULT 'Y',
    `EFFECTIVE_DATE` DATETIME,
    `EXPIRY_DATE` DATETIME,
    `PPDM_GUID` VARCHAR(40),
    `REMARK` VARCHAR(2000),
    `SOURCE` VARCHAR(40),
    `ROW_CHANGED_BY` VARCHAR(30),
    `ROW_CHANGED_DATE` DATETIME,
    `ROW_CREATED_BY` VARCHAR(30),
    `ROW_CREATED_DATE` DATETIME,
    `ROW_EFFECTIVE_DATE` DATETIME,
    `ROW_EXPIRY_DATE` DATETIME,
    `ROW_QUALITY` VARCHAR(40)
);
