CREATE TABLE `RESERVOIR_FORECAST_PROPERTIES`
(
    `RESERVOIR_FORECAST_PROPERTIES_ID` VARCHAR(40) NOT NULL,
    `INITIAL_PRESSURE` DECIMAL(18,2),
    `PERMEABILITY` DECIMAL(18,2),
    `THICKNESS` DECIMAL(18,2),
    `DRAINAGE_RADIUS` DECIMAL(18,2),
    `WELLBORE_RADIUS` DECIMAL(18,2),
    `FORMATION_VOLUME_FACTOR` DECIMAL(18,2),
    `OIL_VISCOSITY` DECIMAL(18,2),
    `TOTAL_COMPRESSIBILITY` DECIMAL(18,2),
    `POROSITY` DECIMAL(18,2),
    `SKIN_FACTOR` DECIMAL(18,2),
    `GAS_SPECIFIC_GRAVITY` DECIMAL(18,2),
    `TEMPERATURE` DECIMAL(18,2),
    `ACTIVE_IND` VARCHAR(1) DEFAULT 'Y',
    `PPDM_GUID` VARCHAR(40),
    `REMARK` VARCHAR(2000),
    `SOURCE` VARCHAR(40),
    `ROW_CREATED_DATE` DATETIME,
    `ROW_CREATED_BY` VARCHAR(30),
    `ROW_CHANGED_DATE` DATETIME,
    `ROW_CHANGED_BY` VARCHAR(30),
    `ROW_EFFECTIVE_DATE` DATETIME,
    `ROW_EXPIRY_DATE` DATETIME,
    `ROW_QUALITY` VARCHAR(40),
    `AREA_ID` VARCHAR(40),
    `AREA_TYPE` VARCHAR(50),
    `BUSINESS_ASSOCIATE_ID` VARCHAR(40),
    `EFFECTIVE_DATE` DATETIME,
    `EXPIRY_DATE` DATETIME
);
