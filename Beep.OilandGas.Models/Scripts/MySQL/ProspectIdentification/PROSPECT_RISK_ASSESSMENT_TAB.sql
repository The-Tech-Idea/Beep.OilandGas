CREATE TABLE `PROSPECT_RISK_ASSESSMENT`
(
    `PROSPECT_ID` VARCHAR(40),
    `ASSESSMENT_ID` VARCHAR(40),
    `RISK_MODEL_TYPE` VARCHAR(50),
    `TRAP_RISK` DECIMAL(18,2),
    `RESERVOIR_RISK` DECIMAL(18,2),
    `SEAL_RISK` DECIMAL(18,2),
    `SOURCE_RISK` DECIMAL(18,2),
    `TIMING_RISK` DECIMAL(18,2),
    `OVERALL_GEOLOGICAL_RISK` DECIMAL(18,2),
    `LOW_ESTIMATE_OIL` DECIMAL(18,2),
    `BEST_ESTIMATE_OIL` DECIMAL(18,2),
    `HIGH_ESTIMATE_OIL` DECIMAL(18,2),
    `LOW_ESTIMATE_GAS` DECIMAL(18,2),
    `BEST_ESTIMATE_GAS` DECIMAL(18,2),
    `HIGH_ESTIMATE_GAS` DECIMAL(18,2),
    `OIL_PRICE` DECIMAL(18,2),
    `GAS_PRICE` DECIMAL(18,2),
    `DEVELOPMENT_COST` DECIMAL(18,2),
    `RISKED_OIL_VOLUME` DECIMAL(18,2),
    `RISKED_GAS_VOLUME` DECIMAL(18,2),
    `UNRISKED_OIL_VOLUME` DECIMAL(18,2),
    `UNRISKED_GAS_VOLUME` DECIMAL(18,2),
    `EMV` DECIMAL(18,2),
    `NPV` DECIMAL(18,2),
    `RISK_CATEGORY` VARCHAR(255),
    `RISK_FACTORS` VARCHAR(255),
    `RECOMMENDATIONS` VARCHAR(255),
    `ASSESSMENT_DATE` DATETIME,
    `ASSESSOR` VARCHAR(255),
    `DESCRIPTION` VARCHAR(2000),
    `ACTIVE_IND` VARCHAR(1) DEFAULT 'Y',
    `EFFECTIVE_DATE` DATETIME,
    `EXPIRY_DATE` DATETIME,
    `PPDM_GUID` VARCHAR(40),
    `REMARK` VARCHAR(2000),
    `SOURCE` VARCHAR(40),
    `ROW_CHANGED_BY` VARCHAR(30),
    `ROW_CHANGED_DATE` DATETIME,
    `ROW_CREATED_BY` VARCHAR(30),
    `ROW_CREATED_DATE` DATETIME,
    `ROW_EFFECTIVE_DATE` DATETIME,
    `ROW_EXPIRY_DATE` DATETIME,
    `ROW_QUALITY` VARCHAR(40)
);
