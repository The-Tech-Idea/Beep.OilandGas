CREATE TABLE `PROSPECT_SOURCE_ROCK`
(
    `PROSPECT_ID` VARCHAR(40),
    `SOURCE_ID` VARCHAR(40),
    `STRAT_UNIT_ID` VARCHAR(40),
    `TOC` DECIMAL(18,2),
    `HI` DECIMAL(18,2),
    `KEROGEN_TYPE` VARCHAR(50),
    `VITRINITE_REFLECTANCE` DECIMAL(18,2),
    `TMAX` DECIMAL(18,2),
    `MATURITY_STAGE` VARCHAR(255),
    `GENERATION_POTENTIAL` DECIMAL(18,6),
    `EXPULSION_EFFICIENCY` DECIMAL(18,2),
    `MIGRATION_DISTANCE` DECIMAL(18,6),
    `MIGRATION_DISTANCE_OUOM` VARCHAR(255),
    `DESCRIPTION` VARCHAR(2000),
    `ACTIVE_IND` VARCHAR(1) DEFAULT 'Y',
    `EFFECTIVE_DATE` DATETIME,
    `EXPIRY_DATE` DATETIME,
    `PPDM_GUID` VARCHAR(40),
    `REMARK` VARCHAR(2000),
    `SOURCE` VARCHAR(40),
    `ROW_CHANGED_BY` VARCHAR(30),
    `ROW_CHANGED_DATE` DATETIME,
    `ROW_CREATED_BY` VARCHAR(30),
    `ROW_CREATED_DATE` DATETIME,
    `ROW_EFFECTIVE_DATE` DATETIME,
    `ROW_EXPIRY_DATE` DATETIME,
    `ROW_QUALITY` VARCHAR(40)
);
