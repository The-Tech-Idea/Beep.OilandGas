CREATE TABLE `PIPELINE_ROUTE`
(
    `PIPELINE_ID` VARCHAR(40),
    `ROUTE_SEQ_NO` DECIMAL(18,2) NOT NULL,
    `LATITUDE` DECIMAL(18,2) NOT NULL,
    `LONGITUDE` DECIMAL(18,2) NOT NULL,
    `ELEVATION` DECIMAL(18,2) NOT NULL,
    `ELEVATION_OUOM` VARCHAR(255),
    `STATION_MILEPOST` DECIMAL(18,2) NOT NULL,
    `STATION_MILEPOST_OUOM` VARCHAR(255),
    `LINEAR_REFERENCE` DECIMAL(18,2) NOT NULL,
    `COORD_SYSTEM_ID` VARCHAR(40),
    `LOCAL_COORD_SYSTEM_ID` VARCHAR(40),
    `COORD_ACQUISITION_ID` VARCHAR(40),
    `DESCRIPTION` VARCHAR(2000),
    `ACTIVE_IND` VARCHAR(1) DEFAULT 'Y',
    `EFFECTIVE_DATE` DATETIME,
    `EXPIRY_DATE` DATETIME,
    `PPDM_GUID` VARCHAR(40),
    `REMARK` VARCHAR(2000),
    `SOURCE` VARCHAR(40),
    `ROW_CHANGED_BY` VARCHAR(30),
    `ROW_CHANGED_DATE` DATETIME,
    `ROW_CREATED_BY` VARCHAR(30),
    `ROW_CREATED_DATE` DATETIME,
    `ROW_EFFECTIVE_DATE` DATETIME,
    `ROW_EXPIRY_DATE` DATETIME,
    `ROW_QUALITY` VARCHAR(40)
);
