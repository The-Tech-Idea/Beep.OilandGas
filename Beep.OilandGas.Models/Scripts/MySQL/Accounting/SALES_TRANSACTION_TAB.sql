CREATE TABLE `SALES_TRANSACTION`
(
    `SALES_TRANSACTION_ID` VARCHAR(40) NOT NULL,
    `RUN_TICKET_ID` VARCHAR(40),
    `SALES_AGREEMENT_ID` VARCHAR(40),
    `CUSTOMER_BA_ID` VARCHAR(40),
    `SALES_DATE` DATETIME,
    `NET_VOLUME` DECIMAL(18,2),
    `PRICE_PER_BARREL` DECIMAL(18,2),
    `TOTAL_AMOUNT` DECIMAL(18,2),
    `TOTAL_COSTS` DECIMAL(18,2),
    `TOTAL_TAXES` DECIMAL(18,2),
    `NET_REVENUE` DECIMAL(18,2),
    `STATUS` VARCHAR(50),
    `APPROVAL_STATUS` VARCHAR(50),
    `ACTIVE_IND` VARCHAR(1) DEFAULT 'Y',
    `PPDM_GUID` VARCHAR(40),
    `REMARK` VARCHAR(2000),
    `SOURCE` VARCHAR(40),
    `ROW_CREATED_DATE` DATETIME,
    `ROW_CREATED_BY` VARCHAR(30),
    `ROW_CHANGED_DATE` DATETIME,
    `ROW_CHANGED_BY` VARCHAR(30),
    `ROW_EFFECTIVE_DATE` DATETIME,
    `ROW_EXPIRY_DATE` DATETIME
);
