CREATE TABLE "PROSPECT_WORKFLOW_STAGE"
(
    "PROSPECT_ID" VARCHAR(40),
    "STAGE_SEQ_NO" NUMERIC(18,2) NOT NULL,
    "WORKFLOW_STAGE" VARCHAR(255),
    "STAGE_START_DATE" TIMESTAMP,
    "STAGE_END_DATE" TIMESTAMP,
    "STAGE_OUTCOME" VARCHAR(255),
    "NEXT_STAGE" VARCHAR(255),
    "APPROVALS_REQUIRED" VARCHAR(255),
    "DESCRIPTION" VARCHAR(2000),
    "ACTIVE_IND" VARCHAR(1) DEFAULT 'Y',
    "EFFECTIVE_DATE" TIMESTAMP,
    "EXPIRY_DATE" TIMESTAMP,
    "PPDM_GUID" VARCHAR(40),
    "REMARK" VARCHAR(2000),
    "SOURCE" VARCHAR(40),
    "ROW_CHANGED_BY" VARCHAR(30),
    "ROW_CHANGED_DATE" TIMESTAMP,
    "ROW_CREATED_BY" VARCHAR(30),
    "ROW_CREATED_DATE" TIMESTAMP,
    "ROW_EFFECTIVE_DATE" TIMESTAMP,
    "ROW_EXPIRY_DATE" TIMESTAMP,
    "ROW_QUALITY" VARCHAR(40)
);
