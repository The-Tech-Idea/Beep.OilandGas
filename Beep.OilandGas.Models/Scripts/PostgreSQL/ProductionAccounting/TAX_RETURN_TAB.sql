CREATE TABLE "TAX_RETURN"
(
    "TAX_RETURN_ID" VARCHAR(40) NOT NULL,
    "RETURN_NUMBER" VARCHAR(255),
    "TAX_TYPE" VARCHAR(50),
    "RETURN_PERIOD_START" TIMESTAMP,
    "RETURN_PERIOD_END" TIMESTAMP,
    "FILING_DATE" TIMESTAMP,
    "TOTAL_TAX_LIABILITY" NUMERIC(18,2),
    "TAX_PAID" NUMERIC(18,2),
    "TAX_DUE" NUMERIC(18,2),
    "STATUS" VARCHAR(50),
    "DESCRIPTION" VARCHAR(2000),
    "ACTIVE_IND" VARCHAR(1) DEFAULT 'Y',
    "PPDM_GUID" VARCHAR(40),
    "REMARK" VARCHAR(2000),
    "SOURCE" VARCHAR(40),
    "ROW_QUALITY" VARCHAR(40),
    "ROW_CREATED_BY" VARCHAR(30),
    "ROW_CREATED_DATE" TIMESTAMP,
    "ROW_CHANGED_BY" VARCHAR(30),
    "ROW_CHANGED_DATE" TIMESTAMP,
    "ROW_EFFECTIVE_DATE" TIMESTAMP,
    "ROW_EXPIRY_DATE" TIMESTAMP,
    "ROW_ID" VARCHAR(40)
);
