CREATE TABLE "ACCOUNTING_AMORTIZATION"
(
    "ACCOUNTING_AMORTIZATION_ID" VARCHAR(40) NOT NULL,
    "PROPERTY_ID" VARCHAR(40),
    "ROW_QUALITY" VARCHAR(40),
    "WELL_ID" VARCHAR(40),
    "POOL_ID" VARCHAR(40),
    "FIELD_ID" VARCHAR(40),
    "PERIOD_START_DATE" TIMESTAMP,
    "PERIOD_END_DATE" TIMESTAMP,
    "CAPITALIZED_COST" NUMERIC(18,2) NOT NULL,
    "PRODUCTION_BOE" NUMERIC(18,2),
    "TOTAL_RESERVES_BOE" NUMERIC(18,2),
    "AMORTIZATION_AMOUNT" NUMERIC(18,2) NOT NULL,
    "AMORTIZATION_METHOD" VARCHAR(255),
    "DESCRIPTION" VARCHAR(2000),
    "ACTIVE_IND" VARCHAR(1) DEFAULT 'Y',
    "PPDM_GUID" VARCHAR(40),
    "REMARK" VARCHAR(2000),
    "SOURCE" VARCHAR(40),
    "ROW_CHANGED_BY" VARCHAR(30),
    "ROW_CHANGED_DATE" TIMESTAMP,
    "ROW_CREATED_BY" VARCHAR(30),
    "ROW_CREATED_DATE" TIMESTAMP,
    "ROW_EFFECTIVE_DATE" TIMESTAMP,
    "ROW_EXPIRY_DATE" TIMESTAMP,
    "ROW_ID" VARCHAR(40)
);
