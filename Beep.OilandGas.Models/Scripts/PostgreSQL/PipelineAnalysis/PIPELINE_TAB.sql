CREATE TABLE "PIPELINE"
(
    "PIPELINE_ID" VARCHAR(40) NOT NULL,
    "PIPELINE_NAME" VARCHAR(255),
    "PIPELINE_SHORT_NAME" VARCHAR(255),
    "PIPELINE_TYPE" VARCHAR(50),
    "PIPELINE_STATUS" VARCHAR(50),
    "REGULATORY_ID" VARCHAR(40),
    "PRIMARY_FIELD_ID" VARCHAR(40),
    "CURRENT_OPERATOR" VARCHAR(255),
    "DESCRIPTION" VARCHAR(2000),
    "INSTALLATION_DATE" TIMESTAMP,
    "COMMISSIONING_DATE" TIMESTAMP,
    "DECOMMISSIONING_DATE" TIMESTAMP,
    "ABANDONED_DATE" TIMESTAMP,
    "ACTIVE_DATE" TIMESTAMP,
    "INACTIVE_DATE" TIMESTAMP,
    "DESIGN_PRESSURE" NUMERIC(18,2) NOT NULL,
    "DESIGN_PRESSURE_OUOM" VARCHAR(255),
    "DESIGN_TEMPERATURE" NUMERIC(18,2) NOT NULL,
    "DESIGN_TEMPERATURE_OUOM" VARCHAR(255),
    "COORD_SYSTEM_ID" VARCHAR(40),
    "LOCAL_COORD_SYSTEM_ID" VARCHAR(40),
    "COORD_ACQUISITION_ID" VARCHAR(40),
    "LATITUDE" NUMERIC(18,2) NOT NULL,
    "LONGITUDE" NUMERIC(18,2) NOT NULL,
    "ELEVATION" NUMERIC(18,2) NOT NULL,
    "ELEVATION_OUOM" VARCHAR(255),
    "ACTIVE_IND" VARCHAR(1) DEFAULT 'Y',
    "EFFECTIVE_DATE" TIMESTAMP,
    "EXPIRY_DATE" TIMESTAMP,
    "PPDM_GUID" VARCHAR(40),
    "REMARK" VARCHAR(2000),
    "SOURCE" VARCHAR(40),
    "ROW_CHANGED_BY" VARCHAR(30),
    "ROW_CHANGED_DATE" TIMESTAMP,
    "ROW_CREATED_BY" VARCHAR(30),
    "ROW_CREATED_DATE" TIMESTAMP,
    "ROW_EFFECTIVE_DATE" TIMESTAMP,
    "ROW_EXPIRY_DATE" TIMESTAMP,
    "ROW_QUALITY" VARCHAR(40),
    "DIAMETER" NUMERIC(18,2),
    "DIAMETER_OUOM" VARCHAR(255),
    "LENGTH" NUMERIC(18,2),
    "LENGTH_OUOM" VARCHAR(255)
);
