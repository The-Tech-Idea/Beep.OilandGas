CREATE TABLE "PIPELINE_CAPACITY_RESULT"
(
    "PIPELINE_CAPACITY_RESULT_ID" VARCHAR(40) NOT NULL,
    "PIPELINE_PROPERTIES_ID" VARCHAR(40),
    "MAXIMUM_FLOW_RATE" NUMERIC(18,2),
    "PRESSURE_DROP" NUMERIC(18,2),
    "FLOW_VELOCITY" NUMERIC(18,2),
    "REYNOLDS_NUMBER" NUMERIC(18,2),
    "FRICTION_FACTOR" NUMERIC(18,2),
    "PRESSURE_GRADIENT" NUMERIC(18,2),
    "OUTLET_PRESSURE" NUMERIC(18,2),
    "ACTIVE_IND" VARCHAR(1) DEFAULT 'Y',
    "PPDM_GUID" VARCHAR(40),
    "REMARK" VARCHAR(2000),
    "SOURCE" VARCHAR(40),
    "ROW_CREATED_DATE" TIMESTAMP,
    "ROW_CREATED_BY" VARCHAR(30),
    "ROW_CHANGED_DATE" TIMESTAMP,
    "ROW_CHANGED_BY" VARCHAR(30),
    "ROW_EFFECTIVE_DATE" TIMESTAMP,
    "ROW_EXPIRY_DATE" TIMESTAMP,
    "ROW_QUALITY" VARCHAR(40)
);
