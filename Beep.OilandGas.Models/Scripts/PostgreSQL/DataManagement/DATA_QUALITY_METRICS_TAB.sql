CREATE TABLE "DATA_QUALITY_METRICS"
(
    "METRICS_ID" VARCHAR(40),
    "TABLE_NAME" VARCHAR(255),
    "TOTAL_RECORDS" INTEGER,
    "COMPLETE_RECORDS" INTEGER,
    "INCOMPLETE_RECORDS" INTEGER,
    "COMPLETENESS_SCORE" NUMERIC(18,2),
    "ACCURACY_SCORE" NUMERIC(18,2),
    "CONSISTENCY_SCORE" NUMERIC(18,2),
    "OVERALL_QUALITY_SCORE" NUMERIC(18,2),
    "METRICS_DATE" TIMESTAMP,
    "FIELD_METRICS_JSON" VARCHAR(255),
    "ACTIVE_IND" VARCHAR(1) DEFAULT 'Y',
    "PPDM_GUID" VARCHAR(40),
    "SOURCE" VARCHAR(40),
    "REMARK" VARCHAR(2000),
    "ROW_CREATED_DATE" TIMESTAMP,
    "ROW_CREATED_BY" VARCHAR(30),
    "ROW_CHANGED_DATE" TIMESTAMP,
    "ROW_CHANGED_BY" VARCHAR(30),
    "ROW_EFFECTIVE_DATE" TIMESTAMP,
    "ROW_EXPIRY_DATE" TIMESTAMP,
    "ROW_QUALITY" VARCHAR(40),
    "AREA_ID" VARCHAR(40),
    "AREA_TYPE" VARCHAR(50),
    "BUSINESS_ASSOCIATE_ID" VARCHAR(40),
    "EFFECTIVE_DATE" TIMESTAMP,
    "EXPIRY_DATE" TIMESTAMP
);
