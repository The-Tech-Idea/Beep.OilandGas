raiserror ('CREATING TABLE PIPELINE_FLOW_ANALYSIS_RESULT', 10,1) with nowait

CREATE TABLE PIPELINE_FLOW_ANALYSIS_RESULT
(
    PIPELINE_FLOW_ANALYSIS_RESULT_ID NVARCHAR(40) NOT NULL,
    PIPELINE_PROPERTIES_ID NVARCHAR(40),
    FLOW_RATE NUMERIC(18,2),
    PRESSURE_DROP NUMERIC(18,2),
    FLOW_VELOCITY NUMERIC(18,2),
    REYNOLDS_NUMBER NUMERIC(18,2),
    FRICTION_FACTOR NUMERIC(18,2),
    PRESSURE_GRADIENT NUMERIC(18,2),
    OUTLET_PRESSURE NUMERIC(18,2),
    FLOW_REGIME NVARCHAR(255),
    ACTIVE_IND NVARCHAR(1) DEFAULT 'Y',
    PPDM_GUID NVARCHAR(40),
    REMARK NVARCHAR(2000),
    SOURCE NVARCHAR(40),
    ROW_CREATED_DATE DATETIME,
    ROW_CREATED_BY NVARCHAR(30),
    ROW_CHANGED_DATE DATETIME,
    ROW_CHANGED_BY NVARCHAR(30),
    ROW_EFFECTIVE_DATE DATETIME,
    ROW_EXPIRY_DATE DATETIME,
    ROW_QUALITY NVARCHAR(40)
);
