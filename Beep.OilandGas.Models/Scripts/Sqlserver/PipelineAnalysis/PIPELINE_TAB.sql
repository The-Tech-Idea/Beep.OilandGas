raiserror ('CREATING TABLE PIPELINE', 10,1) with nowait

CREATE TABLE PIPELINE
(
    PIPELINE_ID NVARCHAR(40) NOT NULL,
    PIPELINE_NAME NVARCHAR(255),
    PIPELINE_SHORT_NAME NVARCHAR(255),
    PIPELINE_TYPE NVARCHAR(50),
    PIPELINE_STATUS NVARCHAR(50),
    REGULATORY_ID NVARCHAR(40),
    PRIMARY_FIELD_ID NVARCHAR(40),
    CURRENT_OPERATOR NVARCHAR(255),
    DESCRIPTION NVARCHAR(2000),
    INSTALLATION_DATE DATETIME,
    COMMISSIONING_DATE DATETIME,
    DECOMMISSIONING_DATE DATETIME,
    ABANDONED_DATE DATETIME,
    ACTIVE_DATE DATETIME,
    INACTIVE_DATE DATETIME,
    DESIGN_PRESSURE NUMERIC(18,2) NOT NULL,
    DESIGN_PRESSURE_OUOM NVARCHAR(255),
    DESIGN_TEMPERATURE NUMERIC(18,2) NOT NULL,
    DESIGN_TEMPERATURE_OUOM NVARCHAR(255),
    COORD_SYSTEM_ID NVARCHAR(40),
    LOCAL_COORD_SYSTEM_ID NVARCHAR(40),
    COORD_ACQUISITION_ID NVARCHAR(40),
    LATITUDE NUMERIC(18,2) NOT NULL,
    LONGITUDE NUMERIC(18,2) NOT NULL,
    ELEVATION NUMERIC(18,2) NOT NULL,
    ELEVATION_OUOM NVARCHAR(255),
    ACTIVE_IND NVARCHAR(1) DEFAULT 'Y',
    EFFECTIVE_DATE DATETIME,
    EXPIRY_DATE DATETIME,
    PPDM_GUID NVARCHAR(40),
    REMARK NVARCHAR(2000),
    SOURCE NVARCHAR(40),
    ROW_CHANGED_BY NVARCHAR(30),
    ROW_CHANGED_DATE DATETIME,
    ROW_CREATED_BY NVARCHAR(30),
    ROW_CREATED_DATE DATETIME,
    ROW_EFFECTIVE_DATE DATETIME,
    ROW_EXPIRY_DATE DATETIME,
    ROW_QUALITY NVARCHAR(40),
    DIAMETER NUMERIC(18,2),
    DIAMETER_OUOM NVARCHAR(255),
    LENGTH NUMERIC(18,2),
    LENGTH_OUOM NVARCHAR(255)
);
