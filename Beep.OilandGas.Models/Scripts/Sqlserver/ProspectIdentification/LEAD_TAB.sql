raiserror ('CREATING TABLE LEAD', 10,1) with nowait

CREATE TABLE LEAD
(
    LEAD_ID NVARCHAR(40) NOT NULL,
    LEAD_NAME NVARCHAR(255),
    LEAD_SHORT_NAME NVARCHAR(255),
    FIELD_ID NVARCHAR(40),
    PLAY_ID NVARCHAR(40),
    LEAD_STATUS NVARCHAR(50),
    DESCRIPTION NVARCHAR(2000),
    LEAD_SOURCE NVARCHAR(40),
    INITIAL_ASSESSMENT_DATE DATETIME,
    PROMOTED_TO_PROSPECT_DATE DATETIME,
    REJECTED_DATE DATETIME,
    REJECTION_REASON NVARCHAR(255),
    LATITUDE NUMERIC(18,2),
    LONGITUDE NUMERIC(18,2),
    ELEVATION NUMERIC(18,2),
    ELEVATION_OUOM NVARCHAR(255),
    LOCATION_DESCRIPTION NVARCHAR(2000),
    INITIAL_RISK_ASSESSMENT NVARCHAR(255),
    INITIAL_VOLUME_ESTIMATE_OIL NUMERIC(18,2),
    INITIAL_VOLUME_ESTIMATE_GAS NUMERIC(18,2),
    INITIAL_VOLUME_ESTIMATE_OUOM NVARCHAR(255),
    PROMOTED_TO_PROSPECT_ID NVARCHAR(40),
    ACTIVE_IND NVARCHAR(1) DEFAULT 'Y',
    EFFECTIVE_DATE DATETIME,
    EXPIRY_DATE DATETIME,
    PPDM_GUID NVARCHAR(40),
    REMARK NVARCHAR(2000),
    SOURCE NVARCHAR(40),
    ROW_CHANGED_BY NVARCHAR(30),
    ROW_CHANGED_DATE DATETIME,
    ROW_CREATED_BY NVARCHAR(30),
    ROW_CREATED_DATE DATETIME,
    ROW_EFFECTIVE_DATE DATETIME,
    ROW_EXPIRY_DATE DATETIME,
    ROW_QUALITY NVARCHAR(40)
);
