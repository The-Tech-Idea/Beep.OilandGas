raiserror ('CREATING TABLE COMPRESSOR_PRESSURE_RESULT', 10,1) with nowait

CREATE TABLE COMPRESSOR_PRESSURE_RESULT
(
    COMPRESSOR_PRESSURE_RESULT_ID NVARCHAR(40) NOT NULL,
    COMPRESSOR_OPERATING_CONDITIONS_ID NVARCHAR(40),
    REQUIRED_DISCHARGE_PRESSURE NUMERIC(18,2),
    COMPRESSION_RATIO NUMERIC(18,6),
    REQUIRED_POWER NUMERIC(18,2),
    DISCHARGE_TEMPERATURE NUMERIC(18,2),
    IS_FEASIBLE BIT,
    ACTIVE_IND NVARCHAR(1) DEFAULT 'Y',
    PPDM_GUID NVARCHAR(40),
    REMARK NVARCHAR(2000),
    SOURCE NVARCHAR(40),
    ROW_CREATED_DATE DATETIME,
    ROW_CREATED_BY NVARCHAR(30),
    ROW_CHANGED_DATE DATETIME,
    ROW_CHANGED_BY NVARCHAR(30),
    ROW_EFFECTIVE_DATE DATETIME,
    ROW_EXPIRY_DATE DATETIME,
    ROW_QUALITY NVARCHAR(40)
);
