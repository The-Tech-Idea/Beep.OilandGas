raiserror ('CREATING TABLE ESP_DESIGN_RESULT', 10,1) with nowait

CREATE TABLE ESP_DESIGN_RESULT
(
    ESP_DESIGN_RESULT_ID NVARCHAR(40) NOT NULL,
    ESP_DESIGN_PROPERTIES_ID NVARCHAR(40),
    PUMP_STAGES INT,
    REQUIRED_HORSEPOWER NUMERIC(18,2),
    MOTOR_HORSEPOWER NUMERIC(18,2),
    MOTOR_VOLTAGE NUMERIC(18,2),
    MOTOR_CURRENT NUMERIC(18,2),
    CABLE_SIZE INT,
    CABLE_LENGTH NUMERIC(18,2),
    SYSTEM_EFFICIENCY NUMERIC(18,2),
    PUMP_EFFICIENCY NUMERIC(18,2),
    MOTOR_EFFICIENCY NUMERIC(18,2),
    POWER_CONSUMPTION NUMERIC(18,2),
    OPERATING_FLOW_RATE NUMERIC(18,2),
    OPERATING_HEAD NUMERIC(18,2),
    ACTIVE_IND NVARCHAR(1) DEFAULT 'Y',
    PPDM_GUID NVARCHAR(40),
    REMARK NVARCHAR(2000),
    SOURCE NVARCHAR(40),
    ROW_CREATED_DATE DATETIME,
    ROW_CREATED_BY NVARCHAR(30),
    ROW_CHANGED_DATE DATETIME,
    ROW_CHANGED_BY NVARCHAR(30),
    ROW_EFFECTIVE_DATE DATETIME,
    ROW_EXPIRY_DATE DATETIME,
    ROW_QUALITY NVARCHAR(40),
    AREA_ID NVARCHAR(40),
    AREA_TYPE NVARCHAR(50),
    BUSINESS_ASSOCIATE_ID NVARCHAR(40),
    EFFECTIVE_DATE DATETIME,
    EXPIRY_DATE DATETIME
);
